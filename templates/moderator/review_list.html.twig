{% extends 'base.html.twig' %}

{% block title %}Gestion des avis
{% endblock %}

{% block body %}
	<div class="ecoride-container rounded p-5 m-3 d-flex flex-column gap-3">
		<div class="mx-auto align-self-start">
			<a href="{{ path('app_moderator') }}" class="btn btn-danger">
				<i class="bi bi-arrow-left-short"></i>
				Retour
			</a>
		</div>
		<h3 class="text-center viga-regular">Liste des avis à vérifier</h3>
		{% for review in allReviews %}
			{% include "partials/reviewcheck.html.twig" %}
		{% endfor %}
		<div class="mx-auto">
			<a href="{{ path('app_moderator') }}" class="btn btn-danger">
				<i class="bi bi-arrow-left-short"></i>
				Retour
			</a>
		</div>
	</div>
{% endblock %}
